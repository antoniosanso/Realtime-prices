# Webshot & Quote Extractor (v3.1)

- Crea sempre la cartella base `webshots/` e una sottocartella con timestamp UTC.
- Salva `run_summary.txt` e, se input vuoto, `NO_DATA.txt`.
- Non fallisce il commit se la cartella è vuota.
- Stampa l'elenco file creati.
